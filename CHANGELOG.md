# Changelog

TranslateR release notes are generated automatically by GitLab CI from commit
subjects since the previous `vX.Y.Z` tag.

See the GitLab or GitHub release pages for per-version changelogs and download
assets.

